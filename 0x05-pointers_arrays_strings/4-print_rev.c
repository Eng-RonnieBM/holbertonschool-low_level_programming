#include "holberton.h"

/**
 *print_rev - check the code for Holberton School students.
 *@s: Parameters*
 * Return: Always void.
 */

void print_rev(char *s)
{
int n;
int d;

for (n = 0; s[n] != '\0'; n++)
d++;
for (d = d; d >= 0; d--)
_putchar(s[d]);
_putchar('\n');
}
